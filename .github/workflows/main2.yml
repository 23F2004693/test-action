name: My First GitHub Action

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 23f2004693@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done step
        run: echo "Action completed!"
